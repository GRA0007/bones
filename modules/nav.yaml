# Module: nav

template: nav.php

fields:
    links:
        type: component-array
        fields:
            text:
                type: string
            link:
                type: internal_link
